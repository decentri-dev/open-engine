[package]
name = "open_engine_core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
alloy = { version = "0.1.3", features = ["serde", "signers", "rlp", "signer-local", "providers", "transport-http"] }

[dev-dependencies]
tokio = { workspace = true }
